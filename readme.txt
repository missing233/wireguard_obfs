1. openwrt_kernel_patch dir: openwrt/target/linux/generic/
2. openwrt_netsupport dir: openwrt/package/kernel/linux/modules/
3. openwrt_package dir:  openwrt/package/wireguard_obf/ [is for latest snapshot only, and added coreutils-shuf depends (for random port on every start).]
4. wireguard_obf-tools have been patched, and need compile & install manually.
